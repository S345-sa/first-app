# The Book

Final, forged text only. Nothing enters this directory except through:

1. the **mastery gate** — the learner restated the material successfully, and
2. the **Forge** — draft → fresh-context Critic → rewrite, minimum two critic
   passes, FORGE-READY verdict (see CLAUDE.md).

One file per chapter: `NN-<chapter-slug>.md`, numbered per `architecture.md`.

This book is a certificate, not a transcript: every paragraph here is material
its reader has already proven they understand — because its reader wrote half of
it with their questions.
